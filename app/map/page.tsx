/* The map itself lives in the layout for this route group. Opening a place
   or a city slides the panel in over a map that never reloaded. */
export const metadata = { title: "The map" };
export default function MapPage() { return null; }
