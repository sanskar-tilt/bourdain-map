/* The map is the page. It lives in the layout, mounted once, so this route
   renders nothing — opening a place or a city slides the panel in over a map
   that never reloaded. */
export default function Home() {
  return null;
}
